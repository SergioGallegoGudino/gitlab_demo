- name: Creacion entorno nginx
  hosts: all
  become: true
  tasks:
    - name: Instalar nginx
      ansible.builtin.dnf:
        name: nginx
        state: present
        
    - name: Crear carpetas
      ansible.builtin.file:
        path: /srv/nginx/pages
        state: directory
        owner: root
